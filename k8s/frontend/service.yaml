apiVersion: v1
kind: Service
metadata:
  name: cisco-frontend-service
  namespace: ciscotechnologies
spec:
  selector:
    app: cisco-frontend
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP