apiVersion: v1
kind: Service
metadata:
  name: cisco-api-service
spec:
  selector:
    app: cisco-api
  ports:
    - port: 5000
      targetPort: 5000
  type: ClusterIP
  # k8s/api/service.yaml — ensure the port has a name