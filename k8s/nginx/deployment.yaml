apiVersion: apps/v1
kind: Deployment
metadata:
  name: cisco-nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cisco-nginx
  template:
    metadata:
      labels:
        app: cisco-nginx
    spec:
      containers:
        - name: nginx
          image: nginx:alpine
          ports:
            - containerPort: 80
          volumeMounts:
            - name: nginx-config
              mountPath: /etc/nginx/conf.d/default.conf
              subPath: nginx.conf
      volumes:
        - name: nginx-config
          configMap:
            name: nginx-config